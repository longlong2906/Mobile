<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        
        <!-- Login Activity - LAUNCHER -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Forgot Password Activity -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Quiz Activity -->
        <activity
            android:name=".QuizActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Result Activity -->
        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Major Detail Activity -->
        <activity
            android:name=".MajorDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- School Detail Activity -->
        <activity
            android:name=".SchoolDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- School Admission Activity -->
        <activity
            android:name=".SchoolAdmissionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- School Majors Activity -->
        <activity
            android:name=".SchoolMajorsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- School Major Detail Activity -->
        <activity
            android:name=".SchoolMajorDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- School Clubs Activity -->
        <activity
            android:name=".SchoolClubsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Chatbot Activity -->
        <activity
            android:name=".ChatbotActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
            
    </application>

</manifest>